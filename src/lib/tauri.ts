// import { invoke, promisified } from 'tauri/api/tauri'
// import { emit, listen } from 'tauri/api/event'

const fn = (...params: any[]) => null as any

const invoke = fn
const promisified = fn
const emit = fn
const listen = fn

export { invoke, promisified, emit, listen }
