{
  "name": "@termy/native",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "onchange 'src/**/*.rs' 'index.d.ts' -- cargo build && napi build",
    "build": "napi build --release"
  },
  "devDependencies": {
    "napi-rs": "latest",
    "onchange": "^7.1.0"
  }
}
