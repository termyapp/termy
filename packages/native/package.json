{
  "name": "@termy/native",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "onchange 'src/**/*.rs' 'index.d.ts' -- yarn build-dev",
    "build": "cargo build --release napi build --release",
    "build-dev": "cargo build && napi build"
  },
  "devDependencies": {
    "napi-rs": "latest",
    "onchange": "^7.1.0"
  }
}
